"""OIDC token verification (issue #43) and the permission framework
(issue #44, FR-44, NFR-20, FR-01, FR-80, FR-81, NFR-06). See
`nptc.auth.permissions` for the PRD Section 4.7 matrix as code,
`nptc.auth.principal` for the resolved-actor type, `nptc.auth.
authorisation` for the check API, and `nptc.auth.grants` for granting/
revoking roles."""
