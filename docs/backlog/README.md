# Backlog

Not yet populated. Lands with Foundation issue F-6: one YAML file per phase
(`foundation.yaml`, `p0.yaml` … `p5.yaml`, `governance.yaml`), plus `backlog_sync.py`
under `scripts/` which renders these into GitHub issues.

This directory, not the GitHub issue body, is the source of truth for what each issue
covers and which of its checklist boxes are ticked. See CONTRIBUTING.md's "Issue
checklists" section.
