# Architecture

Not yet populated. This directory will hold the data model (`data-model.md`), the
component and deployment topology (PRD §14.3 is the starting point), and the contracts
between the API, the worker, and the terminology client (PRD §12, FR-53).

Populated incrementally as the corresponding backlog items land — see the
documentation-impact table in [CONTRIBUTING.md](../../CONTRIBUTING.md) for which
changes require an update here.
