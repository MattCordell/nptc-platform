/**
 * Scaffolding only, proving the Vite/React/TypeScript toolchain — build, lint,
 * typecheck and test — works end to end.
 *
 * The real application shell (routing, the generated OpenAPI client, an
 * accessible component baseline) lands with backlog issue P1-10.
 */
export function App() {
  return (
    <main>
      <h1>NPTC Catalogue Maintenance Platform</h1>
      <p>Pre-alpha. The application shell has not been built yet — see P1-10.</p>
    </main>
  );
}
