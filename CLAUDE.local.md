# Local overrides

## Git workflow

This project requires PR-only merges (see `CLAUDE.md`'s Git / PR workflow section: no
direct pushes to `main`). This overrides the global preference in
`~/.claude/CLAUDE.md` to commit and push directly after changes are accepted — in this
repo, push to a feature branch and open a PR instead.
